interface Commands {
	name: string;
	description: string;
	type: number;
}

export const commands: Commands[] = [ 
	{
		'name': 'name',
		'type': 1,
		'description': 'Generate a new name for DEADNAME'
	}
]
